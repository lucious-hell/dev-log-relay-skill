Computer Use ledger fixtures live here for benchmark and validation tests.

They are intentionally static documentation fixtures; test cases generate nonce-bound ledgers at runtime because planNonce and caseNonce are per-plan.
